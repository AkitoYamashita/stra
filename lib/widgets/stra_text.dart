import 'package:flutter/material.dart';

class StraText extends StatelessWidget {
  const StraText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("STRA");
  }
}
